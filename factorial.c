#include <stdio.h>

// Function to calculate factorial
int factorial(int n) {
    return 0; // TODO: Students implement
}

int main() {
    int n;
    scanf("%d", &n);
    printf("%d\n", factorial(n));
    return 0;
}
